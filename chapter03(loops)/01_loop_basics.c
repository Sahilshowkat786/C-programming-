#include <stdio.h>
// if somrthing needs to be repeated then we use "for" loop
int main(){
    for(int i= 1;i<=8;i++){       // i++ <=>  i=i+1
        printf("i bear witness that allah is one\n");
    }
    return 0;
}